<%= form_for [@group, @membership] do |f| %>
  <%= f.error_messages %>
  <div class="field">
    <%= f.label :user_email %><br />
    <%= f.text_field :user_email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
